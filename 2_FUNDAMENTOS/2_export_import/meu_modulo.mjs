function soma(a, b) {
  console.log(a + b);
}

export default soma;

/*A partir de um arquivo mjs, podemos exportar módulos internos de uma forma mais moderna
se usassemos o require e um arquivo .js, estariamos importando o módulo inteiro, agora com o
recurso do ES6 do javascript podemos importar uma função específica de um módulo interno, o que é uma
forma mais inteligente de se usar*/
