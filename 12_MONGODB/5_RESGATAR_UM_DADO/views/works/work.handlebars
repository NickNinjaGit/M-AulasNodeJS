<div class="work-page">
    <a href="/">Voltar</a>
    <h1>{{work.name}}</h1>
    <p>{{work.description}}</p>
    <img src="{{work.image}}" alt="{{work.name}}">
    <p>R${{work.price}}</p>
    <p>{{work.timestamp}}</p>
</div>